# Тестовое задание — Web-программист

## Frontend + Backend (PHP, JS, MySQL)

Данный проект представляет собой выполнение тестового задания на позицию Web-программиста.
Фронтенд реализован на **HTML, CSS, JavaScript**, а бэкенд — на **PHP без фреймворков**.
База данных работает на **MySQL (XAMPP)**.

---

## 🚀 Функционал

### **Frontend**

* Выполнена адаптивная верстка Figma-макета:
  [https://www.figma.com/design/m6v9Mv3yu14WggKGRMk68d/alkovrach.ru](https://www.figma.com/design/m6v9Mv3yu14WggKGRMk68d/alkovrach.ru)
* Верстка реализована **до блока «О Клинике» включительно**.
* Реализована форма с полями:

  * Имя
  * Email
  * Телефон
* Отправка формы происходит **без перезагрузки страницы** (через `fetch`).

---

### **Backend (PHP)**

* Реализован обработчик формы на PHP.
* Использован **один из принципов ООП (инкапсуляция):**
  создан класс для обработки формы и валидации данных.
* Используются следующие проверки:

  * Валидация email
  * Валидация телефона (формат + обязательность)
* Реализована проверка на дублирование:

  * Если запись с теми же `name + email + phone` уже существует
    **и была отправлена в последние 5 минут**,
    заявка не принимается.
* В зависимости от результата:

  * При успехе — форма скрывается, выводится сообщение об успешной отправке.
  * При ошибке — над формой выводится сообщение об ошибке.

---

## 🗄 База данных

Используется MySQL.
Таблица `applications` имеет структуру:

| Поле       | Тип                            |
| ---------- | ------------------------------ |
| id         | INT AUTO_INCREMENT PRIMARY KEY |
| name       | VARCHAR(255)                   |
| email      | VARCHAR(255)                   |
| phone      | VARCHAR(50)                    |
| created_at | TIMESTAMP                      |

---

## ⚙ Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone <ссылка-на-репозиторий>
```

### 2. Настроить сервер

Используется **XAMPP**, поэтому необходимо:

* поместить проект в папку

  ```
  htdocs/
  ```

* запустить **Apache** в панели XAMPP и отдельно **MySQL**

### 3. Создать базу данных

В командной строке выполнить:

```sql
CREATE DATABASE form_db;
USE form_db;

CREATE TABLE applications (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    phone VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4. Открыть проект в браузере

Перейти по адресу:

```
http://localhost/название-папки/
```
---

## 🧪 Использованные технологии

* **HTML5**
* **CSS3**
* **JavaScript (fetch, DOM)**
* **PHP (ООП + процедурные части)**
* **MySQL**
* **XAMPP**
